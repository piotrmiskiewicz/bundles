apiVersion: v1
entries:
  redis:
    - name: redis
      description: "Redis service."
      version: 0.0.3
  lambda-rc-error:
    - name: lambda-rc-error
      description: "Lambda redis client."
      version: 0.0.1
